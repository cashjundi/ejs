<!DOCTYPE html>
<html>
<head>
  <title>CFX.re Lookup</title>
  <style>
    body { font-family: sans-serif; text-align: center; margin-top: 50px; }
    img { border-radius: 50%; margin-top: 20px; }
  </style>
</head>
<body>
  <h1>CFX.re Username Lookup</h1>
  <form method="GET" action="/api/lookup">
    <input type="text" name="username" placeholder="Enter CFX username" required>
    <button type="submit">Lookup</button>
  </form>

  <% if (error) { %>
    <p style="color: red;"><%= error %></p>
  <% } else if (profile) { %>
    <h2><%= profile.name || profile.username %> (@<%= profile.username %>)</h2>
    <img src="<%= profile.avatar %>" width="128">
    <p><%= profile.bio || "No bio available" %></p>
    <p>Joined: <%= new Date(profile.created_at).toLocaleDateString() %></p>
    <% if (profile.website) { %>
      <p>Website: <%= profile.website %></p>
    <% } %>
  <% } %>
</body>
</html>
